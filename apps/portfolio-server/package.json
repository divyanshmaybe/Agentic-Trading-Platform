{
  "name": "@portfolio/server",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio Server - FastAPI server with NSE pipeline integration",
  "scripts": {
    "setup": "python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt",
    "dev": "python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt --upgrade --upgrade-strategy only-if-needed && uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  },
  "dependencies": {},
  "devDependencies": {}
}
