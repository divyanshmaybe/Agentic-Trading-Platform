{
	"name": "@portfolio/server",
	"version": "1.0.0",
	"private": true,
	"description": "Portfolio Server",
	"scripts": {
		"setup": "python -m pip install -r requirements.txt && prisma generate",
		"prisma:generate": "prisma generate",
		"prisma:studio": "prisma studio",
		"dev": "PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
		"celery": "echo '⚠️  Run from root: pnpm celery' to see all worker pools in separate panels",
		"celery:all": "pnpm run --parallel celery:trading celery:pipeline celery:allocation celery:market celery:general celery:beat",
		"celery:trading": "CELERY_WORKER_RUNNING=1 PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py celery -A celery_app:celery_app worker --loglevel=info -Q trading --concurrency=8 --hostname=trading@%h --max-tasks-per-child=200 --time-limit=60",
		"celery:pipeline": "CELERY_WORKER_RUNNING=1 PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py celery -A celery_app:celery_app worker --loglevel=info -Q pipelines --concurrency=4 --hostname=pipeline@%h --max-tasks-per-child=50 --time-limit=3600",
		"celery:allocation": "CELERY_WORKER_RUNNING=1 PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py celery -A celery_app:celery_app worker --loglevel=info -Q allocations --concurrency=2 --hostname=allocation@%h --max-tasks-per-child=100 --time-limit=900",
		"celery:market": "CELERY_WORKER_RUNNING=1 PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py celery -A celery_app:celery_app worker --loglevel=info -Q market,tokens --concurrency=4 --hostname=market@%h --max-tasks-per-child=500 --time-limit=10",
		"celery:general": "CELERY_WORKER_RUNNING=1 PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py celery -A celery_app:celery_app worker --loglevel=info -Q general,risk,orders --concurrency=2 --hostname=general@%h --max-tasks-per-child=200 --time-limit=300",
		"celery:beat": "CELERY_WORKER_RUNNING=1 PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py celery -A celery_app:celery_app beat --loglevel=info",
		"streaming:risk": "PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py python -m workers.streaming_risk_monitor",
		"streaming:orders": "PYTHONPATH=../..:../../shared/py:../../middleware:../../middleware/py python -m workers.streaming_order_monitor",
		"test": "python -c \"import sys; sys.path.insert(0, '../../shared/py'); sys.path.insert(0, '../../middleware/py'); sys.path.insert(0, '../..'); import pytest; pytest.main(['tests/', '-v', '--tb=short'])\"",
		"test:cov": "python -c \"import sys; sys.path.insert(0, '../../shared/py'); sys.path.insert(0, '../../middleware/py'); sys.path.insert(0, '../..'); import pytest; pytest.main(['tests/', '-v', '--tb=short', '--cov=.', '--cov-report=term-missing', '--cov-report=html'])\"",
		"test:watch": "python -c \"import sys; sys.path.insert(0, '../../shared/py'); sys.path.insert(0, '../../middleware/py'); sys.path.insert(0, '../..'); import pytest; pytest.main(['tests/', '-v', '--tb=short', '--watch'])\"",
		"test:unit": "python -c \"import sys; sys.path.insert(0, '../../shared/py'); sys.path.insert(0, '../../middleware/py'); sys.path.insert(0, '../..'); import pytest; pytest.main(['tests/', '-v', '--tb=short', '-m', 'not integration'])\"",
		"test:integration": "python -c \"import sys; sys.path.insert(0, '../../shared/py'); sys.path.insert(0, '../../middleware/py'); sys.path.insert(0, '../..'); import pytest; pytest.main(['tests/', '-v', '--tb=short', '-m', 'integration'])\"",
		"studio": "prisma studio --port 5555",
		"studio:kafka": "streamlit run ../../kafkaStudio.py"
	},
	"dependencies": {},
	"devDependencies": {}
}